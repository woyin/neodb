; pytest configuration for neodb (the main Django project).
; Dependencies are declared in the repository root pyproject.toml and installed
; into the single shared venv; run pytest with this directory as cwd.
[pytest]
DJANGO_SETTINGS_MODULE = tests.settings
python_files = tests/*.py
testpaths = tests
