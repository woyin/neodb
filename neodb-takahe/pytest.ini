; pytest configuration for neodb-takahe (the Fediverse / Mastodon API app).
; Dependencies are declared in the repository root pyproject.toml and installed
; into the single shared venv; only the takahe-specific test settings live here.
[pytest]
addopts = --tb=short --ds=takahe.settings --import-mode=importlib
asyncio_default_fixture_loop_scope = function
filterwarnings =
    ignore:There is no current event loop
    ignore:No directory at
    ignore:DateTimeField Post.created
    ignore:'index_together' is deprecated
    ignore:Deprecated call to
    ignore:pkg_resources is deprecated as an API
    ignore:Type google._upb
    ignore:Marks applied to fixtures
